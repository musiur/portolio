const Footer = () => {
  return (
    <footer className="container section text-center border-t border-border/50">
      All rights reserved @{new Date().getFullYear()}
    </footer>
  );
};

export default Footer;
